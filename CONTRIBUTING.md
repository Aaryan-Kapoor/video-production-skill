# Contributing

For now this repository is private. The quality bar is simple:

- Keep `SKILL.md` concise and action-oriented.
- Put long guidance in `references/` and deterministic helpers in `scripts/`.
- Test scripts before changing them.
- Validate and package before release:

```bash
./scripts/package.sh
```
